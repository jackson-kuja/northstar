# Northstar Backend Environment Variables
GEMINI_API_KEY=your-gemini-api-key-here
GCP_PROJECT=northstar-agent
# Optional: stable default browser agent model for local runs
BROWSER_AGENT_MODEL=gemini-2.5-flash
# Optional: set to 1 only when using a Computer Use-capable preview model
# BROWSER_AGENT_ENABLE_COMPUTER_USE=1
